# Copiar este archivo como .env.local y ajustar valores
# cp env.example .env.local

# Backend API URL (solo para desarrollo local)
# En produccion (EC2 con nginx proxy) NO se necesita esta variable.
NEXT_PUBLIC_BACKEND_IP=http://localhost:8000
